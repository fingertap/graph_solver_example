Note that to compile the code you should include the third party path. See makefile for example.
Note that before running, you should set the environment variable:
- Linux: 
```LD_LIBRARY_PATH=${PWD}/src/third_party/lib```
- Mac:
```DYLD_LIBRARY_PATH=${PWD}/src/third_party/lib```
